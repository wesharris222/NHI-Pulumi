fastapi>=0.115,<1.0
uvicorn[standard]>=0.32,<1.0
pydantic>=2.10,<3.0
requests>=2.32,<3.0
python-dotenv>=1.0,<2.0

# Python 3.8/3.9 can't natively eval PEP-604 union syntax (`str | None`) at
# pydantic class construction time. Future-annotations defers evaluation but
# pydantic forces it. This backport patches typing.get_type_hints to handle
# the new syntax on older interpreters.
eval_type_backport>=0.2.0; python_version < "3.10"
